<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chi tiết Section</title>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
          sans-serif;
        margin: 2em;
        background-color: #f7f7f7;
        color: #333;
      }
      .container {
        background-color: white;
        padding: 2em;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        max-width: 800px;
        margin: auto;
      }
      h1 {
        color: #1a1a1a;
        border-bottom: 2px solid #007bff;
        padding-bottom: 10px;
      }
      p {
        line-height: 1.6;
      }
      strong {
        color: #007bff;
      }
      .error {
        color: #d8000c;
        background-color: #ffd2d2;
        padding: 1em;
        border-radius: 8px;
        font-weight: bold;
      }
      .back-link {
        display: inline-block;
        margin-top: 2em;
        text-decoration: none;
        color: #fff;
        background-color: #007bff;
        padding: 10px 15px;
        border-radius: 5px;
        transition: background-color 0.2s;
      }
      .back-link:hover {
        background-color: #0069d9;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Chi tiết Thiết bị 📝</h1>
      <div id="sectionDetail"></div>
      <a href="/devices" class="back-link">← Quay lại danh sách</a>
    </div>

    <script>
      document.addEventListener("DOMContentLoaded", () => {
        const sectionDetailDiv = document.getElementById("sectionDetail");

        const path = window.location.pathname;
        const id = path.split("/").pop();
        if (!id) {
          sectionDetailDiv.innerHTML = `<p class="error">Không tìm thấy ID của thiết bị trong URL.</p>`;
          return;
        }

        const accessToken = localStorage.getItem("access_token");

        if (!accessToken) {
          sectionDetailDiv.innerHTML = `<p class="error">Lỗi: Không tìm thấy access_token. Vui lòng đăng nhập lại.</p>`;
          return;
        }

        const config = {
          headers: {
            Authorization: `Bearer ${accessToken}`,
          },
        };
        console.log(id);
        const apiUrl = `http://localhost:3000/api/devices/${id}`;
        axios
          .get(apiUrl, config)
          .then((response) => {
            const device = response.data.data;
            console.log(device);
            sectionDetailDiv.innerHTML = `
                        <p><strong>ID:</strong> ${device._id}</p>
                        <p><strong>Tên Thiết bị:</strong> ${
                          device.deviceName
                        }</p>
                        <p><strong>Mô tả:</strong> ${
                          device.deviceDescription
                        }</p>
                        <p><strong>Giá:</strong> ${device.cost}</p>
                        <p><strong>Giảm giá:</strong> ${device.saleOff}</p>
                        <p><strong>Thiết bị nổi bật:</strong> ${
                          device.famouse ? "Có" : "Không"
                        }</p>
                        <p><strong>Model:</strong> ${
                          device.model?.modelName || "N/A"
                        }</p>
                        
                    `;
          })
          .catch((error) => {
            console.error("Lỗi khi tải chi tiết device:", error);
            let errorMessage = `Không thể tải dữ liệu cho device với ID: ${id}.`;
            if (
              error.response &&
              (error.response.status === 401 || error.response.status === 403)
            ) {
              errorMessage =
                "Lỗi xác thực. Token không hợp lệ hoặc đã hết hạn.";
            }
            sectionDetailDiv.innerHTML = `<p class="error">${errorMessage}</p>`;
          });
      });
    </script>
  </body>
</html>
